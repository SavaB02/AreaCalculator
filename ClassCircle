Public Class clsCircle  'creating class for circle shape
    Inherits ClsShape   'Inherits abstract class clsShape

    'Making new sub that will override Area sub from clsShape to add a new formula
    Public Overrides Sub Area()    'adding new formula, because to calculate an area for the circle we have to use a different one
        Result = Var1 * Math.PI 'formula Math.PI that stands for PI number value
    End Sub
End Class
